steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/frontend:$BUILD_ID'
      - '-t'
      - 'gcr.io/$PROJECT_ID/frontend:latest'
      - '--build-arg'
      - 'NEXT_PUBLIC_API_URL=https://saas.tf/api/v1'
      - '--build-arg'
      - 'NEXT_PUBLIC_APP_NAME=saas.tf'
      - '--build-arg'
      - 'NEXT_PUBLIC_DOMAIN=saas.tf'
      - '--build-arg'
      - 'NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_51SnFhqDhWqOk1oY31r3aT87udkD6DfmSAwmnee8z21aSAV1T8BZPY8E2F5DWt1I94bAtQ0LzfpsWJKrjFNE0ixpS00OymdcVdA'
      - '.'

  # Push the Docker images to GCR
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'gcr.io/$PROJECT_ID/frontend'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'frontend'
      - '--image=gcr.io/$PROJECT_ID/frontend:$BUILD_ID'
      - '--region=us-central1'
      - '--platform=managed'
      - '--min-instances=0'
      - '--max-instances=10'
      - '--cpu=1'
      - '--memory=512Mi'
      - '--timeout=60s'
      - '--concurrency=100'
      - '--allow-unauthenticated'
      - '--set-env-vars=NODE_ENV=production,NEXT_PUBLIC_API_URL=https://saas.tf/api/v1,NEXT_PUBLIC_APP_NAME=saas.tf,NEXT_PUBLIC_DOMAIN=saas.tf,NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_51SnFhqDhWqOk1oY31r3aT87udkD6DfmSAwmnee8z21aSAV1T8BZPY8E2F5DWt1I94bAtQ0LzfpsWJKrjFNE0ixpS00OymdcVdA'

images:
  - 'gcr.io/$PROJECT_ID/frontend:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/frontend:latest'

options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'  # 20 minutes
